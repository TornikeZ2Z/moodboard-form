/* ============================================================
   CONFIGURATION — the only file you need to touch for setup
   ============================================================ */
const CONFIG = {
  // 1) Get a free access key at https://web3forms.com (takes 2 minutes:
  //    enter the e-mail that should RECEIVE the answers, confirm, paste key here)
  WEB3FORMS_KEY: "YOUR_WEB3FORMS_ACCESS_KEY_HERE",

  // 2) Where the client's answers are sent (shown in the e-mail subject)
  DESIGNER_NAME: "Mariam",
  BRAND_NAME: "MARIAM",

  // 3) Default language: "ge" | "en" | "ru"
  DEFAULT_LANG: "ge",

  // 4) Image locations (relative to index.html). To replace any picture,
  //    just drop a new file with the same name into assets/…
  ASSETS: "assets"
};
